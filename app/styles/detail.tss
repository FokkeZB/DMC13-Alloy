"Window": {
	backgroundColor: "#ccc"
}

".wrap": {
	top: 10, right: 10, left: 10,
	height: Ti.UI.SIZE,
	
	backgroundColor: '#fff',
	borderRadius: 5
}

"#html": {
	top: 40, left: 70, right: 10, bottom: 10,
	height: Ti.UI.SIZE,
	
	disableBounce: true,
	enableZoomControls: false,
	hideLoadIndicator: true
}

".tests": {
	layout: 'vertical'
}

".test": {
	bottom: 10,
	height: Ti.UI.SIZE
}

".test[platform=ios]": {
	color: 'blue'
}